
# This project will only install docker and run a container with Apache web server on a fresh Ubuntu VM!!!
 

Base Script name = apache2-container.sh

Docker install script file name = docker-installation.sh

Image Name = new-image

Container name = new-app

Docker file name = Dockerfile

Code file = index.html
